from setuptools import setup

setup(
    name="map_app",
    version="0.1",
    packages=["map_app"],
    package_dir={"": "src"},
    # entry_points={
    # 'console_scripts': [
    #     'cli = map_app.cli.main:main',
    # ],
# },
)
