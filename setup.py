from setuptools import setup

setup(
    name="mmap",
    version="0.1",
    packages=["mmap"],
    package_dir={"": "src"},
    # entry_points={
    # 'console_scripts': [
    #     'cli = map_app.cli.main:main',
    # ],
# },
)
